{
  "name": "require-worker",
  "version": "1.0.0-4",
  "description": "require with a fork and a promise",
  "main": "index.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "example": "node ./examples/example1.js",
    "test": "mocha -t 10000 --recursive tests/**/*.js",
    "test-watch": "mocha -t 10000 --recursive tests/**/*.js --watch",
    "test-watch-inspect": "mocha -t 10000 --recursive tests/**/*.js --watch --inspect"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Unchosen/require-worker"
  },
  "author": "unchosen.aus@gmail.com",
  "license": "MIT",
  "dependencies": {
    "underscore": "^1.8.3"
  },
  "devDependencies": {
    "chai": "^4.0.2",
    "mocha": "^3.4.2"
  }
}
