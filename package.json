{
  "name": "require-worker",
  "version": "1.0.0-2",
  "description": "require with a fork and a promise",
  "main": "index.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "start": "node index.js",
    "example": "node ./examples/example1.js",
    "test": "mocha -t 10000 --recursive tests/**/*.js",
    "test-watch": "mocha -t 10000 --recursive tests/**/*.js --watch"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Unchosen/require-worker"
  },
  "author": "unchosen.aus@gmail.com",
  "license": "MIT",
  "dependencies": {
    "underscore": "^1.8.3"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0"
  }
}
